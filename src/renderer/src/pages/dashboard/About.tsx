import DashboardComp from "@renderer/components/layout/Dashboard.comp";

const About = () => {
    return (
        <div id="about-page">
            <DashboardComp context="about" />
        </div>
    );
};

export default About;
