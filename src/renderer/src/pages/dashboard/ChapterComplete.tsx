import DashboardComp from "@renderer/components/layout/Dashboard.comp";

const ChapterComplete = () => {
    return (
        <div id="chapterComplete-page">
            <DashboardComp context="chapter-complete" />
        </div>
    );
};

export default ChapterComplete;
