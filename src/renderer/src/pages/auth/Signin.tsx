import Auth from "@renderer/components/layout/Auth.comp";

const Signin = () => {
    return (
        <div id="signin-note">
            <Auth context="signin" />
        </div>
    );
};

export default Signin;
