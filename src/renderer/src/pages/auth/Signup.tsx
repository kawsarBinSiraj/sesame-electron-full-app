import Auth from "@renderer/components/layout/Auth.comp";

const Signup = () => {
    return (
        <div id="signup-note">
            <Auth context="signup" />
        </div>
    );
};

export default Signup;
