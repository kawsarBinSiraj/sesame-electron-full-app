import Auth from "@renderer/components/layout/Auth.comp";

const CreateAnotherAccount = () => {
    return (
        <div id="createAnother-account-note">
            <Auth context="another-signup" />
        </div>
    );
};

export default CreateAnotherAccount;
